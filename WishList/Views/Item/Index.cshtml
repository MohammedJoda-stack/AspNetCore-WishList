@model IEnumerable<WishList.Models.Item>
<h1>WishList</h1>
<a asp-action="create">Add item</a>
<ul>
    @foreach (var item in Model)
    {
        <li>@item.Description</li>
        <a asp-action="delete" asp-all-route-id="@item.Id">Delete</a>
    }
</ul>